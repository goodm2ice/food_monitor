import { memo } from 'react'
import { ScrollView } from 'react-native'
import { Text } from 'react-native-paper'

export const DailyPFC = memo(() => {
    
    return (
        <ScrollView>
            <Text>Halo</Text>
        </ScrollView>
    )
})

export default DailyPFC
